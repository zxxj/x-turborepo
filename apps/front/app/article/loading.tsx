const XLoader = () => {
  return (
    <div className="h-screen flex items-center justify-center">
      <span
        className="text-6xl font-semibold animate-bounce  tracking-wide
                       [animation-duration:2.2s] 
                       [animation-timing-function:ease-in-out]"
      >
        xin
      </span>
    </div>
  );
};

export default XLoader;
